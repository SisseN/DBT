version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    loaded_at_field: _etl_loaded_at
    freshness:
      warn_after: {count: 2, period: day}
      error_after: {count: 2, period: day}
    
    tables:
      - name: customers
        description: Raw data table with unique customers 1 per row
        freshness: null
      - name: orders
        description: RAW data table with unique Orders 1 per row